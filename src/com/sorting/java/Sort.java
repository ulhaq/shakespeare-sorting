package com.sorting.java;

import java.util.List;

public interface Sort {
    static void sorting(List<String> arr) {
    }

    static long startTimer() {
        return System.currentTimeMillis();
    }

    static long endTimer() {
        return System.currentTimeMillis();
    }
}
